import Level from '../Level';
import { Point } from '../../Types';

export default class LevelTest extends Level {
    name = 'Test';
    spawn: Point = [200, 50];

    OnInit() {
        // TODO: ...
    }
}
